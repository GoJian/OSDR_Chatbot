# Backend RAG + API. HTTP to ollama/OSDR uses the Python stdlib (urllib).
# The frontend additionally needs Node.js (see frontend/README).
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
chromadb>=0.5.0
pydantic>=2.6.0
